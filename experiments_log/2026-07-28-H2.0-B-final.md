# H2.0-B final verdict - Simple MLP Monitor (ablation of slot attention)

> Date: 2026-07-28
> Status: H2.0-B NOT supported (per pre-registered rule, t<2.0).
>         But direction is clearly positive (delta=+43.4), n=5 too
>         small for the variance level. Same pattern as H2.0-A.

## 1. H2.0-B results (n=5 per arm, 100K PPO, LunarLander)

| Method | n | Mean | Std | Per-seed |
|--------|---|------|-----|----------|
| Simple MLP (trained) | 5 | 117.7 | 54.2 | [148, 171, 136, 32, 101] |
| Simple MLP (random) | 5 | 74.3 | 62.5 | [86, -5, 145, 118, 27] |

**Delta: +43.4, Welch t: 1.174**
**Pre-registered verdict: NOT supported** (delta > +10 BUT t < 2.0)

## 2. Comparison: H2.0-A vs H2.0-B (both new directions)

| Test | Intervention | Architecture | Delta | t | Verdict |
|------|--------------|---------------|-------|---|---------|
| H2.0-A | Exploration bonus | forward model | **+40.2** | 1.30 | NOT supported |
| **H2.0-B** | **Reward shaping** | **Simple MLP (no slot)** | **+43.4** | **1.17** | **NOT supported** |

Both new direction tests show:
- Direction is positive (delta ~+40)
- t is 1.1-1.3 (not significant at n=5)
- High variance (std 54-65)

The "direction" is similar across 2 different use cases (forward
model exploration, simple MLP reward shaping) and 2 different
architectures (forward model MLP, simple failure MLP). This
suggests the direction is the **abstraction (any informative
signal)** rather than the specific architecture.

## 3. Cross-test comparison: 6 tests now

| Test | Intervention | n | Delta | t | Verdict |
|------|---------------|---|-------|---|---------|
| H1 | Y1.3 reward shaping, 100K (slot) | 15/10 | +13.6 | 0.78 | NOT supported |
| H3 | Y1.3 reward shaping, 500K (slot) | 5/5 | -53.1 | -2.16 | NOT supported, ACTIVE HARM |
| H1.4 | Y1.4 exploration bonus, 100K (slot) | 5/5 | -25.6 | -1.12 | NOT supported |
| H2.0-A | FM exploration bonus, 100K (FM) | 5/5 | +40.2 | 1.30 | NOT supported |
| **H2.0-B** | **Y1.3 reward shaping, 100K (MLP)** | **5/5** | **+43.4** | **1.17** | **NOT supported** |

Patterns:
- Y1.x (Monitor use cases): deltas -53 to +14, mostly negative
- H2.0 (new direction): deltas +40, positive direction
- n=5 insufficient for any test to reach t > 2.0

## 4. What this means

The 5 pre-registered tests now show:
- Monitor (Y1.x): not useful
- Forward model + simple MLP (H2.0): direction-positive but n=5
  insufficient for significance

A reasonable next step would be to extend H2.0 to n=10 per arm
to confirm or refute the direction-positive signal. But per the
pre-registered protocol, I should NOT silently extend.

## 5. Decision record (DEC-H2.0 v1.0)

H2.0-A: NOT supported (delta=+40.2, t=1.30)
H2.0-B: NOT supported (delta=+43.4, t=1.17)
Combined: Both new direction tests show direction-positive effects
similar in magnitude to Y1.x v1.0 (which was overclaimed).

**RECOMMENDATION**:
- The direction is interesting but the verdict is per the rule
- Future work: H2.1 with n=10 per arm (pre-registration update)
- Without the n=10 update, the Y1.x + H2.0 sub-project is
  definitively closed (5 H tests, 0 supported, 0 direction-confirmed)

## 6. What this means for the paper

- Section 4.10 should be extended to include H2.0-A and H2.0-B
- The Y1.x verdict (Monitor is not useful) is CONFIRMED
- The H2.0 verdict (new direction is direction-positive) is
  INFORMATIVE but UNCONFIRMED
- The paper should report all 5 tests with their pre-registered
  verdicts and the n=5 limitation
